<div class="col-md-1">
</div> 
<div class="col-md-10 main">
      {{#each products}}
        <div class="col-sm-5 col-md-3 product">
          <div class="thumbnail">
            <a href="/products/{{_id}}">
              <img alt="{{name}}" src="{{image}}"></a>
              <div class="caption">
              <h3>{{name}}</h3>
              <p class="price">Rs.{{price}}</p>
              <!-- <form method="POST" action="/cart/{{id}}">
              <label for="quantity">Select quantity</label>
                <select class="form-control product-dropdown" id="quantity" name="quantity">
                  <option>1</option>
                  <option>2</option>
                  <option>3</option>
                  <option>4</option>
                  <option>5</option>
                </select>
                <button class="btn btn-default noPad" type="submit" value="Submit">Add To Cart</button>
                </form> -->
              <p><a href="/products/{{_id}}" class="btn btn-primary btn-block view-product" role="button">View Product</a>
            </div>
          </div>
        </div>
      {{/each}}
    </div>

<div class="col-md-1">
</div>
